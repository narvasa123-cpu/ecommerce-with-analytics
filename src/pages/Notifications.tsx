import { useState } from 'react';
import { Bell, CheckCheck, CircleAlert, Info, PackageCheck } from 'lucide-react';
import { useNotifications } from '@/hooks/useNotifications';
import type { Profile } from '@/types';

export default function NotificationsPage({ user }: { user: Profile | null }) { const { notifications, unreadCount, loading, error, markRead, markAllRead } = useNotifications(user?.id); const [selected, setSelected] = useState<string | null>(null); const iconFor = (type: string) => type.includes('STATUS') || type.includes('DELIVERY') ? <PackageCheck size={18} /> : type.includes('LOW') ? <CircleAlert size={18} /> : <Info size={18} />; return <div className="mx-auto max-w-3xl space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-wider text-blue-600">Workspace updates</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-900">Notifications</h1><p className="mt-1 text-slate-500">Important updates about orders, delivery, and inventory.</p></div>{unreadCount > 0 && <button onClick={() => void markAllRead()} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-50"><CheckCheck size={16} /> Mark all read</button>}</div>{error && <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{loading ? <div className="flex min-h-64 items-center justify-center text-slate-500">Loading notifications…</div> : <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="divide-y divide-slate-100">{notifications.length ? notifications.map((notification) => <button key={notification.id} onClick={() => { setSelected(selected === notification.id ? null : notification.id); if (!notification.is_read) void markRead(notification.id); }} className={`flex w-full items-start gap-4 p-5 text-left transition hover:bg-slate-50 ${!notification.is_read ? 'bg-blue-50/40' : ''}`}><span className={`mt-0.5 flex h-10 w-10 shrink-0 items-center justify-center rounded-xl ${notification.is_read ? 'bg-slate-100 text-slate-500' : 'bg-blue-100 text-blue-700'}`}>{iconFor(notification.type)}</span><span className="min-w-0 flex-1"><span className="flex items-start justify-between gap-3"><span className="font-semibold text-slate-900">{notification.title}</span>{!notification.is_read && <span className="mt-1 h-2 w-2 shrink-0 rounded-full bg-blue-600" />}</span><span className="mt-1 block text-sm text-slate-500">{notification.message || 'You have a new account update.'}</span><span className="mt-2 block text-xs text-slate-400">{new Intl.DateTimeFormat('en-PH', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(notification.created_at))}</span>{selected === notification.id && notification.related_id && <span className="mt-3 block rounded-lg bg-slate-100 px-3 py-2 text-xs text-slate-600">Reference: {notification.related_id}</span>}</span></button>) : <div className="flex flex-col items-center px-6 py-16 text-center"><Bell className="text-slate-300" size={38} /><p className="mt-4 font-semibold text-slate-900">You’re all caught up</p><p className="mt-1 text-sm text-slate-500">New updates will appear here automatically.</p></div>}</div></div>}</div>; }
