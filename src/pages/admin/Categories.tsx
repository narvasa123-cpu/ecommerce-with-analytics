import { useEffect, useState } from 'react';
import { Plus, RefreshCw, Archive, Save } from 'lucide-react';
import { createCategory, listCategories, updateCategory, archiveCategory } from '@/services/categories';
import type { Category } from '@/types';

export default function AdminCategories() { const [items, setItems] = useState<Category[]>([]); const [editing, setEditing] = useState<string | null>(null); const [form, setForm] = useState({ name: '', description: '', image_url: '' }); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState('');
 const load = async () => { setLoading(true); try { setItems(await listCategories(false)); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to load categories.'); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []);
 const save = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(''); try { const saved = editing ? await updateCategory(editing, form) : await createCategory(form); setItems((current) => editing ? current.map((item) => item.id === saved.id ? saved : item) : [saved, ...current]); setEditing(null); setForm({ name: '', description: '', image_url: '' }); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to save category.'); } finally { setSaving(false); } };
 const archive = async (id: string) => { try { await archiveCategory(id); setItems((current) => current.map((item) => item.id === id ? { ...item, is_active: false } : item)); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to archive category.'); } };
 return <div className="mx-auto max-w-6xl space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-wider text-blue-600">Commerce</p><h1 className="mt-1 text-3xl font-bold text-slate-900">Categories</h1><p className="mt-1 text-slate-500">Organize the catalog and keep storefront navigation clear.</p></div><button onClick={() => void load()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-sm font-semibold"><RefreshCw size={16}/> Refresh</button></div>{error && <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<form onSubmit={(e) => void save(e)} className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-[1fr_1.4fr_1fr_auto]"><input required value={form.name} onChange={(e) => setForm({...form,name:e.target.value})} placeholder="Category name" aria-label="Category name" className="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"/><input value={form.description} onChange={(e) => setForm({...form,description:e.target.value})} placeholder="Description" aria-label="Category description" className="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"/><input value={form.image_url} onChange={(e) => setForm({...form,image_url:e.target.value})} placeholder="Image URL (optional)" aria-label="Category image URL" className="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"/><button disabled={saving} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{editing ? <Save size={16}/> : <Plus size={16}/>} {editing ? 'Save' : 'Add'}</button></form><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Category</th><th className="px-5 py-4">Description</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{loading ? <tr><td colSpan={4} className="p-10 text-center text-slate-500">Loading categories…</td></tr> : items.map((item) => <tr key={item.id}><td className="px-5 py-4 font-semibold text-slate-900">{item.name}</td><td className="px-5 py-4 text-sm text-slate-500">{item.description || '—'}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${item.is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500'}`}>{item.is_active ? 'Active' : 'Archived'}</span></td><td className="px-5 py-4 text-right"><button onClick={() => { setEditing(item.id); setForm({name:item.name,description:item.description || '',image_url:item.image_url || ''}); }} className="mr-3 text-sm font-semibold text-blue-600">Edit</button>{item.is_active && <button onClick={() => void archive(item.id)} aria-label={`Archive ${item.name}`} className="text-slate-500"><Archive size={17}/></button>}</td></tr>)}</tbody></table></div></div></div>;
}
