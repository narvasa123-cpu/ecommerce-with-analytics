import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import { Bell, CheckCheck, ChevronDown, LogOut, User } from 'lucide-react';
import { supabase } from '@/lib/supabase';
import { useNotifications } from '@/hooks/useNotifications';
import type { Profile } from '@/types';

interface TopBarProps { user: Profile | null; }
export default function TopBar({ user }: TopBarProps) { const navigate = useNavigate(); const location = useLocation(); const [showDropdown, setShowDropdown] = useState(false); const [showNotifications, setShowNotifications] = useState(false); const { notifications, unreadCount, markRead, markAllRead } = useNotifications(user?.id); const pageName = location.pathname.split('/').filter(Boolean).pop() || 'dashboard'; const pageTitle = pageName.charAt(0).toUpperCase() + pageName.slice(1); const handleLogout = async () => { await supabase.auth.signOut(); navigate('/login'); }; const basePath = user?.role === 'ADMIN' ? '/admin' : user?.role === 'STAFF' ? '/staff' : user?.role === 'RIDER' ? '/rider' : '/customer'; return <header className="flex min-h-[82px] items-center justify-between border-b border-slate-200 bg-white px-4 sm:px-8"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-400">{user?.role === 'ADMIN' ? 'Admin workspace' : 'Store workspace'}</p><h1 className="mt-1 text-xl font-bold tracking-tight text-slate-950">{pageTitle}</h1></div><div className="flex items-center gap-2 sm:gap-4"><div className="relative"><button aria-label={`${unreadCount} unread notifications`} aria-expanded={showNotifications} onClick={() => { setShowNotifications(!showNotifications); setShowDropdown(false); }} className="relative flex h-11 w-11 items-center justify-center rounded-xl text-slate-500 transition hover:bg-slate-100 hover:text-slate-900"><Bell size={19} aria-hidden="true" />{unreadCount > 0 && <span className="absolute right-2.5 top-2.5 flex h-4 min-w-4 items-center justify-center rounded-full bg-blue-600 px-1 text-[10px] font-bold text-white ring-2 ring-white">{unreadCount > 9 ? '9+' : unreadCount}</span>}</button>{showNotifications && <div className="absolute right-0 z-50 mt-3 w-[min(22rem,calc(100vw-2rem))] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xl shadow-slate-900/10"><div className="flex items-center justify-between border-b border-slate-100 p-4"><div><p className="font-bold text-slate-900">Notifications</p><p className="text-xs text-slate-500">{unreadCount ? `${unreadCount} unread update${unreadCount === 1 ? '' : 's'}` : 'All caught up'}</p></div>{unreadCount > 0 && <button onClick={() => void markAllRead()} className="inline-flex items-center gap-1 text-xs font-semibold text-blue-600 hover:text-blue-700"><CheckCheck size={14} /> Read all</button>}</div><div className="max-h-80 overflow-y-auto">{notifications.slice(0, 5).map((notification) => <button key={notification.id} onClick={() => { if (!notification.is_read) void markRead(notification.id); setShowNotifications(false); navigate(`${basePath}/notifications`); }} className={`flex w-full items-start gap-3 p-4 text-left hover:bg-slate-50 ${!notification.is_read ? 'bg-blue-50/40' : ''}`}><span className="mt-1 h-2 w-2 shrink-0 rounded-full bg-blue-600" /><span className="min-w-0"><span className="block truncate text-sm font-semibold text-slate-900">{notification.title}</span><span className="mt-1 block line-clamp-2 text-xs text-slate-500">{notification.message || 'New account update.'}</span></span></button>)}{notifications.length === 0 && <p className="px-4 py-8 text-center text-sm text-slate-500">No notifications yet.</p>}</div><button onClick={() => { setShowNotifications(false); navigate(`${basePath}/notifications`); }} className="min-h-11 w-full border-t border-slate-100 text-sm font-semibold text-blue-600 hover:bg-slate-50">View all notifications</button></div>}</div><div className="relative"><button aria-expanded={showDropdown} onClick={() => { setShowDropdown(!showDropdown); setShowNotifications(false); }} className="flex min-h-11 items-center gap-2 rounded-xl p-1.5 transition hover:bg-slate-100"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-blue-100 font-bold text-blue-700">{user?.full_name?.charAt(0) || 'A'}</div><span className="hidden text-sm font-semibold text-slate-700 sm:block">{user?.full_name || 'User'}</span><ChevronDown size={16} className="text-slate-400" aria-hidden="true" /></button>{showDropdown && <div className="absolute right-0 z-50 mt-3 w-64 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xl shadow-slate-900/10"><div className="border-b border-slate-100 p-4"><p className="font-semibold text-slate-900">{user?.full_name || 'User'}</p><p className="mt-1 text-sm text-slate-500">{user?.email || 'Account profile'}</p><p className="mt-2 inline-flex rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{user?.role || 'USER'}</p></div><button onClick={() => navigate(`${basePath}/profile`)} className="flex min-h-11 w-full items-center gap-2 px-4 text-sm font-medium text-slate-700 hover:bg-slate-50"><User size={17} aria-hidden="true" /><span>My profile</span></button><button onClick={handleLogout} className="flex min-h-11 w-full items-center gap-2 border-t border-slate-100 px-4 text-sm font-medium text-red-600 hover:bg-red-50"><LogOut size={17} aria-hidden="true" /><span>Sign out</span></button></div>}</div></div></header>; }
